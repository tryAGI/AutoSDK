[
  FullResponse(class)[general],
    Ip(string)[general],
    Bogon(bool)[general],
    Hostname(string)[general],
    City(string)[general],
    Region(string)[general],
    Country(string)[general],
    Loc(string)[general],
    Postal(string)[general],
    Timezone(string)[general],
    Org(string)[general],
    AsnResponse(ref)[general],
    Company(ref)[general],
    Carrier(ref)[general],
    PrivacyResponse(ref)[general],
    DomainsResponse(ref)[general],
  AsnResponse(class)[asn],
    Asn(string)[asn],
    Name(string)[asn],
    Country(string)[asn],
    Allocated(string)[asn],
    Registry(string)[asn],
    Domain(string)[asn],
    NumIps(int)[asn],
    Route(string)[asn],
    AsnResponseType(enum)[asn],
    Prefixes(array)[asn],
      Prefix(ref)[],
    Prefixes6(array)[asn],
      Prefix6(ref)[],
    Peers(array)[asn],
      PeersItem(string)[],
    Upstreams(array)[asn],
      UpstreamsItem(string)[],
    Downstreams(array)[asn],
      DownstreamsItem(string)[],
  Prefix(class)[],
    Netblock(string)[],
    Id(string)[],
    Name(string)[],
    Country(string)[],
    Size(string)[],
    Status(string)[],
    Domain(string)[],
  Prefix6(class)[],
    Netblock(string)[],
    Id(string)[],
    Name(string)[],
    Country(string)[],
    Size(string)[],
    Status(string)[],
    Domain(string)[],
  Company(class)[],
    Name(string)[],
    Domain(string)[],
    CompanyType(enum)[],
  Carrier(class)[],
    Name(string)[],
    Mcc(string)[],
    Mnc(string)[],
  PrivacyResponse(class)[privacy detection],
    Vpn(bool)[privacy detection],
    Proxy(bool)[privacy detection],
    Tor(bool)[privacy detection],
    Hosting(bool)[privacy detection],
    Relay(bool)[privacy detection],
    Service(string)[privacy detection],
  DomainsResponse(class)[domains],
    Ip(string)[domains],
    Page(int)[domains],
    Total(int)[domains],
    Domains(array)[domains],
      DomainsItem(string)[],
  RangesResponse(class)[ranges],
    Domain(string)[ranges],
    NumRanges(string)[ranges],
    Ranges(array)[ranges],
      RangesItem(string)[],
  AbuseResponse(class)[abuse],
    Address(string)[abuse],
    Country(string)[abuse],
    Email(string)[abuse],
    Name(string)[abuse],
    Network(string)[abuse],
    Phone(string)[abuse],
  Error(class)[general, asn, ranges, domains, abuse, privacy detection, single],
    ErrorError1(class)[general, asn, ranges, domains, abuse, privacy detection, single],
      Title(string)[],
      Message(string)[],
  BatchRequest(array)[general],
    BatchRequestItem(string)[general],
  GetInformationByIpIp(string)[general],
  GetAsnAsn(int)[asn],
  GetRangesDomain(string)[ranges],
  GetDomainsIp(string)[domains],
  GetDomainsPage(int)[domains],
  GetDomainsLimit(int)[domains],
  GetAbuseIp(string)[abuse],
  GetPrivacyInformationByIpIp(string)[privacy detection],
  GetIpByIpIp(string)[single],
  GetHostnameByIpIp(string)[single],
  GetCityByIpIp(string)[single],
  GetRegionByIpIp(string)[single],
  GetCountryByIpIp(string)[single],
  GetLocationByIpIp(string)[single],
  GetPostalByIpIp(string)[single],
  GetTimezoneByIpIp(string)[single],
  GetOrganizationByIpIp(string)[single],
  FullResponse(ref)[general],
  Error(ref)[general],
  BatchResponse(class)[general],
    BatchResponse2(class)[general],
  Error(ref)[general],
  FullResponse(ref)[general],
  Error(ref)[general],
  AsnResponse(ref)[asn],
  Error(ref)[asn],
  RangesResponse(ref)[ranges],
  Error(ref)[ranges],
  DomainsResponse(ref)[domains],
  Error(ref)[domains],
  AbuseResponse(ref)[abuse],
  Error(ref)[abuse],
  PrivacyResponse(ref)[privacy detection],
  Error(ref)[privacy detection],
  Error(ref)[privacy detection],
  GetCurrentIpResponse(string)[single],
  Error(ref)[single],
  GetIpByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentHostnameResponse(string)[single],
  Error(ref)[single],
  GetHostnameByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentCityResponse(string)[single],
  Error(ref)[single],
  GetCityByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentRegionResponse(string)[single],
  Error(ref)[single],
  GetRegionByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentCountryResponse(string)[single],
  Error(ref)[single],
  GetCountryByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentLocationResponse(string)[single],
  Error(ref)[single],
  GetLocationByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentPostalResponse(string)[single],
  Error(ref)[single],
  GetPostalByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentTimezoneResponse(string)[single],
  Error(ref)[single],
  GetTimezoneByIpResponse(string)[single],
  Error(ref)[single],
  GetCurrentOrganizationResponse(string)[single],
  Error(ref)[single],
  GetOrganizationByIpResponse(string)[single],
  Error(ref)[single]
]