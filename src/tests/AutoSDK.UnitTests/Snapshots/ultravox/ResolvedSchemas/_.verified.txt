[
  APIKey(class)[api_keys],
    Prefix(string)[api_keys],
    Created(DateTime)[api_keys],
    Creator(string)[api_keys],
    Name(string)[api_keys],
    ExpiryDate(DateTime)[api_keys],
    Revoked(bool)[api_keys],
  APIKeyCreate(class)[api_keys],
    Prefix(string)[api_keys],
    Created(DateTime)[api_keys],
    Creator(string)[api_keys],
    Secret(string)[api_keys],
    Revoked(bool)[api_keys],
    Name(string)[api_keys],
    ExpiryDate(DateTime)[api_keys],
  Account(class)[accounts],
    Name(string)[accounts],
    BillingUrl(string)[accounts],
    FreeTimeUsed(string)[accounts],
    FreeTimeRemaining(string)[accounts],
    HasActiveSubscription(bool)[accounts],
    ActiveCalls(int)[accounts],
    AllowedConcurrentCalls(int)[accounts],
    AllowedVoices(int)[accounts],
  Agent(class)[agents],
    AgentId(string)[agents],
    Name(string)[agents],
    Created(DateTime)[agents],
    UltravoxV1CallTemplate(ref)[agents],
  Call(class)[agents, calls, tools],
    CallId(string)[agents, calls, tools],
    ClientVersion(string)[agents, calls, tools],
    Created(DateTime)[agents, calls, tools],
    Joined(DateTime)[agents, calls, tools],
    Ended(DateTime)[agents, calls, tools],
    EndReason(oneOf)[agents, calls, tools],
      EndReasonEnum(ref)[agents, calls, tools],
      NullEnum(ref)[agents, calls, tools],
    FirstSpeaker_AllOf1Wrapped(allOf)[agents, calls, tools],
      FirstSpeakerEnum(ref)[agents, calls, tools],
    FirstSpeakerSettings_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1FirstSpeakerSettings(ref)[agents, calls, tools],
    InactivityMessages(array)[agents, calls, tools],
      UltravoxV1TimedMessage(ref)[agents, calls, tools],
    InitialOutputMedium_AllOf1Wrapped(allOf)[agents, calls, tools],
      InitialOutputMediumEnum(ref)[agents, calls, tools],
    JoinTimeout(string)[agents, calls, tools],
    JoinUrl(string)[agents, calls, tools],
    LanguageHint(string)[agents, calls, tools],
    MaxDuration(string)[agents, calls, tools],
    Medium_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMedium(ref)[agents, calls, tools],
    Model(string)[agents, calls, tools],
    RecordingEnabled(bool)[agents, calls, tools],
    SystemPrompt(string)[agents, calls, tools],
    Temperature(double)[agents, calls, tools],
    TimeExceededMessage(string)[agents, calls, tools],
    Voice(string)[agents, calls, tools],
    UltravoxV1ExternalVoice(ref)[agents, calls, tools],
    TranscriptOptional(bool)[agents, calls, tools],
    ErrorCount(int)[agents, calls, tools],
    VadSettings_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1VadSettings(ref)[agents, calls, tools],
    ShortSummary(string)[agents, calls, tools],
    Summary(string)[agents, calls, tools],
    CallExperimentalSettings(class)[agents, calls, tools],
    CallMetadata(class)[agents, calls, tools],
      Metadata(string)[agents, calls, tools],
    CallInitialState(class)[agents, calls, tools],
      CallInitialState2(class)[agents, calls, tools],
    CallRequestContext(class)[agents, calls, tools],
  CallStage(class)[calls],
    CallId(string)[calls],
    CallStageId(string)[calls],
    Created(DateTime)[calls],
    InactivityMessages(array)[calls],
      UltravoxV1TimedMessage(ref)[calls],
    LanguageHint(string)[calls],
    Model(string)[calls],
    SystemPrompt(string)[calls],
    Temperature(double)[calls],
    TimeExceededMessage(string)[calls],
    Voice(string)[calls],
    UltravoxV1ExternalVoice(ref)[calls],
    ErrorCount(int)[calls],
    ExperimentalSettings(null)[calls],
    CallStageInitialState(class)[calls],
      CallStageInitialState2(class)[calls],
  CallStatistics(class)[accounts],
    TotalCount(int)[accounts],
    Duration(string)[accounts],
    JoinedCount(int)[accounts],
  CallTombstone(class)[deleted_calls],
    CallId(string)[deleted_calls],
    AccountId(string)[deleted_calls],
    Created(DateTime)[deleted_calls],
    DeletionTime(DateTime)[deleted_calls],
    Joined(DateTime)[deleted_calls],
    Ended(DateTime)[deleted_calls],
    MaxDuration(string)[deleted_calls],
    EndReason2(oneOf)[deleted_calls],
      EndReasonEnum(ref)[deleted_calls],
      NullEnum(ref)[deleted_calls],
    RecordingEnabled(bool)[deleted_calls],
    HadSummary(bool)[deleted_calls],
  CallTool(class)[calls],
    CallToolId(string)[calls],
    ToolId(string)[calls],
    Name(string)[calls],
    UltravoxV1CallTool(ref)[calls],
  CallUsage(class)[accounts],
    AllTime_AllOf1Wrapped(allOf)[accounts],
      CallStatistics(ref)[accounts],
    Daily(array)[accounts],
      DailyCallStatistics(ref)[accounts],
  CorpusUploadsRequest(class)[corpora],
    MimeType(string)[corpora],
  CorpusUploadsResponse(class)[corpora],
    DocumentId(string)[corpora],
    PresignedUrl(Uri)[corpora],
  DailyCallStatistics(class)[accounts],
    TotalCount(int)[accounts],
    Duration(string)[accounts],
    JoinedCount(int)[accounts],
    Date(DateTime)[accounts],
  EndReasonEnum(enum)[agents, calls, deleted_calls, tools],
  EventsEnum(enum)[webhooks],
  FirstSpeakerEnum(enum)[agents, calls, tools],
  InitialOutputMediumEnum(enum)[agents, calls, tools],
  ModelAlias(class)[models],
    Name(string)[models],
  NullEnum(enum)[agents, calls, deleted_calls, tools],
  OwnershipEnum(enum)[tools, voices],
  PaginatedAPIKeyList(class)[api_keys],
    Next(Uri)[api_keys],
    Previous(Uri)[api_keys],
    Results(array)[api_keys],
      APIKey(ref)[api_keys],
    Total(int)[api_keys],
  PaginatedAgentList(class)[agents],
    Next(Uri)[agents],
    Previous(Uri)[agents],
    Results(array)[agents],
      Agent(ref)[agents],
    Total(int)[agents],
  PaginatedCallList(class)[agents, calls],
    Next(Uri)[agents, calls],
    Previous(Uri)[agents, calls],
    Results(array)[agents, calls],
      Call(ref)[agents, calls],
    Total(int)[agents, calls],
  PaginatedCallStageList(class)[calls],
    Next(Uri)[calls],
    Previous(Uri)[calls],
    Results(array)[calls],
      CallStage(ref)[calls],
    Total(int)[calls],
  PaginatedCallTombstoneList(class)[deleted_calls],
    Next(Uri)[deleted_calls],
    Previous(Uri)[deleted_calls],
    Results(array)[deleted_calls],
      CallTombstone(ref)[deleted_calls],
    Total(int)[deleted_calls],
  PaginatedModelAliasList(class)[models],
    Next(Uri)[models],
    Previous(Uri)[models],
    Results(array)[models],
      ModelAlias(ref)[models],
    Total(int)[models],
  PaginatedToolHistoryList(class)[tools],
    Next(Uri)[tools],
    Previous(Uri)[tools],
    Results(array)[tools],
      ToolHistory(ref)[tools],
    Total(int)[tools],
  PaginatedToolList(class)[tools],
    Next(Uri)[tools],
    Previous(Uri)[tools],
    Results(array)[tools],
      Tool(ref)[tools],
    Total(int)[tools],
  PaginatedVoiceList(class)[voices],
    Next(Uri)[voices],
    Previous(Uri)[voices],
    Results(array)[voices],
      Voice(ref)[voices],
    Total(int)[voices],
  PaginatedWebhookList(class)[webhooks],
    Next(Uri)[webhooks],
    Previous(Uri)[webhooks],
    Results(array)[webhooks],
      Webhook(ref)[webhooks],
    Total(int)[webhooks],
  PaginatedultravoxV1CorpusDocumentList(class)[corpora],
    Next(Uri)[corpora],
    Previous(Uri)[corpora],
    Results(array)[corpora],
      UltravoxV1CorpusDocument(ref)[corpora],
    Total(int)[corpora],
  PaginatedultravoxV1CorpusList(class)[corpora],
    Next(Uri)[corpora],
    Previous(Uri)[corpora],
    Results(array)[corpora],
      UltravoxV1Corpus(ref)[corpora],
    Total(int)[corpora],
  PaginatedultravoxV1CorpusSourceList(class)[corpora],
    Next(Uri)[corpora],
    Previous(Uri)[corpora],
    Results(array)[corpora],
      UltravoxV1CorpusSource(ref)[corpora],
    Total(int)[corpora],
  PaginatedultravoxV1MessageList(class)[calls],
    Next(Uri)[calls],
    Previous(Uri)[calls],
    Results(array)[calls],
      UltravoxV1Message(ref)[calls],
    Total(int)[calls],
  PatchedAPIKey(class)[api_keys],
    Prefix(string)[api_keys],
    Created(DateTime)[api_keys],
    Creator(string)[api_keys],
    Name(string)[api_keys],
    ExpiryDate(DateTime)[api_keys],
    Revoked(bool)[api_keys],
  PatchedAgent(class)[agents],
    AgentId(string)[agents],
    Name(string)[agents],
    Created(DateTime)[agents],
    UltravoxV1CallTemplate(ref)[agents],
  PatchedWebhook(class)[webhooks],
    WebhookId(string)[webhooks],
    Created(DateTime)[webhooks],
    Url(Uri)[webhooks],
    Secrets(array)[webhooks],
      SecretsItem(string)[webhooks],
    Events(array)[webhooks],
      EventsEnum(ref)[webhooks],
  Tool(class)[tools],
    ToolId(string)[tools],
    Name(string)[tools],
    Created(DateTime)[tools],
    UltravoxV1BaseToolDefinition(ref)[tools],
    Ownership_AllOf1Wrapped(allOf)[tools],
      OwnershipEnum(ref)[tools],
  ToolDetail(class)[tools],
    ToolId(string)[tools],
    Name(string)[tools],
    Created(DateTime)[tools],
    UltravoxV1BaseToolDefinition(ref)[tools],
    Ownership_AllOf1Wrapped2(allOf)[tools],
      OwnershipEnum(ref)[tools],
    Statistics_AllOf1Wrapped(allOf)[tools],
      ToolStatistics(ref)[tools],
  ToolHistory(class)[tools],
    Call_AllOf1Wrapped(allOf)[tools],
      Call(ref)[tools],
    ErrorCount(int)[tools],
  ToolStatistics(class)[tools],
    ErrorCount(int)[tools],
    TimesCalled(int)[tools],
  Voice(class)[voices],
    VoiceId(string)[voices],
    Name(string)[voices],
    Description(string)[voices],
    PreviewUrl(Uri)[voices],
    Ownership_AllOf1Wrapped3(allOf)[voices],
      OwnershipEnum(ref)[voices],
  Webhook(class)[webhooks],
    WebhookId(string)[webhooks],
    Created(DateTime)[webhooks],
    Url(Uri)[webhooks],
    Secrets(array)[webhooks],
      SecretsItem(string)[webhooks],
    Events(array)[webhooks],
      EventsEnum(ref)[webhooks],
  UltravoxV1CallTemplate(class)[agents],
    Name(string)[agents],
    Created(DateTime)[agents],
    Updated(DateTime)[agents],
    Medium_AllOf1Wrapped2(allOf)[agents],
      UltravoxV1CallMedium(ref)[agents],
    UltravoxV1CallTemplateInitialOutputMedium(enum)[agents],
    JoinTimeout(string)[agents],
    MaxDuration(string)[agents],
    VadSettings_AllOf1Wrapped2(allOf)[agents],
      UltravoxV1VadSettings(ref)[agents],
    RecordingEnabled(bool)[agents],
    FirstSpeakerSettings_AllOf1Wrapped2(allOf)[agents],
      UltravoxV1FirstSpeakerSettings(ref)[agents],
    SystemPrompt(string)[agents],
    Temperature(float)[agents],
    Model(string)[agents],
    Voice(string)[agents],
    LanguageHint(string)[agents],
    TimeExceededMessage(string)[agents],
    InactivityMessages(array)[agents],
      UltravoxV1TimedMessage(ref)[agents],
    SelectedTools(array)[agents],
      UltravoxV1SelectedTool(ref)[agents],
    UltravoxV1CallTemplateContextSchema(class)[agents],
  UltravoxV1Corpus(class)[corpora],
    CorpusId(string)[corpora],
    Created(DateTime)[corpora],
    Name(string)[corpora],
    Description(string)[corpora],
    Stats_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1CorpusStats(ref)[corpora],
  UltravoxV1CorpusDocument(class)[corpora],
    CorpusId(string)[corpora],
    SourceId(string)[corpora],
    DocumentId(string)[corpora],
    Created(DateTime)[corpora],
    MimeType(string)[corpora],
    Metadata_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1CorpusDocumentMetadata(ref)[corpora],
    SizeBytes(string)[corpora],
  UltravoxV1CorpusQueryResult(class)[corpora],
    Content(string)[corpora],
    Score(double)[corpora],
    Citation_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1CorpusQueryResultCitation(ref)[corpora],
  UltravoxV1CorpusSource(class)[corpora],
    CorpusId(string)[corpora],
    SourceId(string)[corpora],
    Created(DateTime)[corpora],
    Name(string)[corpora],
    Description(string)[corpora],
    Stats_AllOf1Wrapped2(allOf)[corpora],
      UltravoxV1SourceStats(ref)[corpora],
    LoadSpec_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1CrawlSpec(ref)[corpora],
    Crawl_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1CrawlSpec(ref)[corpora],
    Upload_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1UploadSpec(ref)[corpora],
  UltravoxV1Message(class)[agents, calls],
    UltravoxV1MessageRole(enum)[agents, calls],
    Text(string)[agents, calls],
    InvocationId(string)[agents, calls],
    ToolName(string)[agents, calls],
    ErrorDetails(string)[agents, calls],
    UltravoxV1MessageMedium(enum)[agents, calls],
    CallStageMessageIndex(int)[agents, calls],
    CallStageId(string)[agents, calls],
    UltravoxV1MessageCallState(class)[agents, calls],
    Timespan_AllOf1Wrapped(allOf)[agents, calls],
      UltravoxV1InCallTimespan(ref)[agents, calls],
  GoogleProtobufValue(class)[agents, calls, tools],
  UltravoxV1AutomaticParameter(class)[agents, calls, tools],
    Name(string)[agents, calls, tools],
    UltravoxV1AutomaticParameterLocation(enum)[agents, calls, tools],
    UltravoxV1AutomaticParameterKnownValue(enum)[agents, calls, tools],
  UltravoxV1BaseClientToolDetails(class)[agents, calls, tools],
  UltravoxV1BaseHttpToolDetails(class)[agents, calls, tools],
    BaseUrlPattern(string)[agents, calls, tools],
    HttpMethod(string)[agents, calls, tools],
  UltravoxV1BaseToolDefinition(class)[agents, calls, tools],
    ModelToolName(string)[agents, calls, tools],
    Description(string)[agents, calls, tools],
    DynamicParameters(array)[agents, calls, tools],
      UltravoxV1DynamicParameter(ref)[agents, calls, tools],
    StaticParameters(array)[agents, calls, tools],
      UltravoxV1StaticParameter(ref)[agents, calls, tools],
    AutomaticParameters(array)[agents, calls, tools],
      UltravoxV1AutomaticParameter(ref)[agents, calls, tools],
    Requirements_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1ToolRequirements(ref)[agents, calls, tools],
    Timeout(string)[agents, calls, tools],
    Precomputable(bool)[agents, calls, tools],
    Http_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1BaseHttpToolDetails(ref)[agents, calls, tools],
    Client_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1BaseClientToolDetails(ref)[agents, calls, tools],
    UltravoxV1BaseToolDefinitionDefaultReaction(enum)[agents, calls, tools],
    StaticResponse_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1StaticToolResponse(ref)[agents, calls, tools],
  UltravoxV1CallMedium(class)[agents, calls, tools],
    WebRtc_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumWebRtcMedium(ref)[agents, calls, tools],
    Twilio_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumTwilioMedium(ref)[agents, calls, tools],
    ServerWebSocket_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumWebSocketMedium(ref)[agents, calls, tools],
    Telnyx_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumTelnyxMedium(ref)[agents, calls, tools],
    Plivo_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumPlivoMedium(ref)[agents, calls, tools],
    Exotel_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1CallMediumExotelMedium(ref)[agents, calls, tools],
  UltravoxV1CallMediumExotelMedium(class)[agents, calls, tools],
  UltravoxV1CallMediumPlivoMedium(class)[agents, calls, tools],
  UltravoxV1CallMediumTelnyxMedium(class)[agents, calls, tools],
  UltravoxV1CallMediumTwilioMedium(class)[agents, calls, tools],
  UltravoxV1CallMediumWebRtcMedium(class)[agents, calls, tools],
  UltravoxV1CallMediumWebSocketMedium(class)[agents, calls, tools],
    InputSampleRate(int)[agents, calls, tools],
    OutputSampleRate(int)[agents, calls, tools],
    ClientBufferSizeMs(int)[agents, calls, tools],
  UltravoxV1CallTool(class)[calls],
    Description(string)[calls],
    DynamicParameters(array)[calls],
      UltravoxV1DynamicParameter(ref)[calls],
    StaticParameters(array)[calls],
      UltravoxV1StaticParameter(ref)[calls],
    AutomaticParameters(array)[calls],
      UltravoxV1AutomaticParameter(ref)[calls],
    Timeout(string)[calls],
    Precomputable(bool)[calls],
    Http_AllOf1Wrapped2(allOf)[calls],
      UltravoxV1HttpCallToolDetails(ref)[calls],
    Client_AllOf1Wrapped2(allOf)[calls],
      UltravoxV1ClientCallToolDetails(ref)[calls],
    UltravoxV1CallToolDefaultReaction(enum)[calls],
    StaticResponse_AllOf1Wrapped2(allOf)[calls],
      UltravoxV1StaticToolResponse(ref)[calls],
  UltravoxV1CartesiaVoice(class)[calls, agents, tools],
    VoiceId(string)[calls, agents, tools],
    Model(string)[calls, agents, tools],
    Speed(float)[calls, agents, tools],
    Emotion(string)[calls, agents, tools],
  UltravoxV1ClientCallToolDetails(class)[calls],
  UltravoxV1CorpusDocumentMetadata(class)[corpora],
    PublicUrl(string)[corpora],
    Language(string)[corpora],
    Title(string)[corpora],
    Description(string)[corpora],
    Published(DateTime)[corpora],
  UltravoxV1CorpusQueryResultCitation(class)[corpora],
    SourceId(string)[corpora],
    DocumentId(string)[corpora],
    PublicUrl(string)[corpora],
    Title(string)[corpora],
  UltravoxV1CorpusStats(class)[corpora],
    UltravoxV1CorpusStatsStatus(enum)[corpora],
    LastUpdated(DateTime)[corpora],
    NumChunks(int)[corpora],
    NumDocs(int)[corpora],
    NumVectors(int)[corpora],
  UltravoxV1CrawlSpec(class)[corpora],
    MaxDocuments(int)[corpora],
    MaxDocumentBytes(int)[corpora],
    RelevantDocumentTypes_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1MimeTypeFilter(ref)[corpora],
    StartUrls(array)[corpora],
      StartUrlsItem(string)[corpora],
    MaxDepth(int)[corpora],
  UltravoxV1DynamicParameter(class)[agents, calls, tools],
    Name(string)[agents, calls, tools],
    UltravoxV1DynamicParameterLocation(enum)[agents, calls, tools],
    UltravoxV1DynamicParameterSchema(class)[agents, calls, tools],
    Required(bool)[agents, calls, tools],
  UltravoxV1ElevenLabsVoice(class)[calls, agents, tools],
    VoiceId(string)[calls, agents, tools],
    Model(string)[calls, agents, tools],
    Speed(float)[calls, agents, tools],
    UseSpeakerBoost(bool)[calls, agents, tools],
    Style(float)[calls, agents, tools],
    SimilarityBoost(float)[calls, agents, tools],
    Stability(float)[calls, agents, tools],
    PronunciationDictionaries(array)[calls, agents, tools],
      UltravoxV1ElevenLabsVoicePronunciationDictionaryReference(ref)[calls, agents, tools],
  UltravoxV1ElevenLabsVoicePronunciationDictionaryReference(class)[calls, agents, tools],
    DictionaryId(string)[calls, agents, tools],
    VersionId(string)[calls, agents, tools],
  UltravoxV1ExternalVoice(class)[calls, agents, tools],
    ElevenLabs_AllOf1Wrapped(allOf)[calls, agents, tools],
      UltravoxV1ElevenLabsVoice(ref)[calls, agents, tools],
    Cartesia_AllOf1Wrapped(allOf)[calls, agents, tools],
      UltravoxV1CartesiaVoice(ref)[calls, agents, tools],
    PlayHt_AllOf1Wrapped(allOf)[calls, agents, tools],
      UltravoxV1PlayHtVoice(ref)[calls, agents, tools],
    Lmnt_AllOf1Wrapped(allOf)[calls, agents, tools],
      UltravoxV1LmntVoice(ref)[calls, agents, tools],
  UltravoxV1FallbackAgentGreeting(class)[agents, calls, tools],
    Delay(string)[agents, calls, tools],
    Text(string)[agents, calls, tools],
  UltravoxV1FirstSpeakerSettings(class)[agents, calls, tools],
    User_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1FirstSpeakerSettingsUserGreeting(ref)[agents, calls, tools],
    Agent_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1FirstSpeakerSettingsAgentGreeting(ref)[agents, calls, tools],
  UltravoxV1FirstSpeakerSettingsAgentGreeting(class)[agents, calls, tools],
    Uninterruptible(bool)[agents, calls, tools],
    Text(string)[agents, calls, tools],
    Delay(string)[agents, calls, tools],
  UltravoxV1FirstSpeakerSettingsUserGreeting(class)[agents, calls, tools],
    Fallback_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1FallbackAgentGreeting(ref)[agents, calls, tools],
  UltravoxV1HeaderApiKeyRequirement(class)[agents, calls, tools],
    Name(string)[agents, calls, tools],
  UltravoxV1HttpAuthRequirement(class)[agents, calls, tools],
    Scheme(string)[agents, calls, tools],
  UltravoxV1HttpCallToolDetails(class)[calls],
    BaseUrlPattern(string)[calls],
    HttpMethod(string)[calls],
    AuthHeaders(array)[calls],
      AuthHeadersItem(string)[calls],
    AuthQueryParams(array)[calls],
      AuthQueryParamsItem(string)[calls],
    CallTokenScopes(array)[calls],
      CallTokenScopesItem(string)[calls],
  UltravoxV1InCallTimespan(class)[agents, calls],
    Start(string)[agents, calls],
    End(string)[agents, calls],
  UltravoxV1LmntVoice(class)[calls, agents, tools],
    VoiceId(string)[calls, agents, tools],
    Model(string)[calls, agents, tools],
    Speed(float)[calls, agents, tools],
    Conversational(bool)[calls, agents, tools],
  UltravoxV1MimeTypeFilter(class)[corpora],
    Include_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1MimeTypeSet(ref)[corpora],
    Exclude_AllOf1Wrapped(allOf)[corpora],
      UltravoxV1MimeTypeSet(ref)[corpora],
  UltravoxV1MimeTypeSet(class)[corpora],
    MimeTypes(array)[corpora],
      MimeTypesItem(string)[corpora],
  UltravoxV1PlayHtVoice(class)[calls, agents, tools],
    UserId(string)[calls, agents, tools],
    VoiceId(string)[calls, agents, tools],
    Model(string)[calls, agents, tools],
    Speed(float)[calls, agents, tools],
    Quality(string)[calls, agents, tools],
    Temperature(float)[calls, agents, tools],
    Emotion(float)[calls, agents, tools],
    VoiceGuidance(float)[calls, agents, tools],
    StyleGuidance(float)[calls, agents, tools],
    TextGuidance(float)[calls, agents, tools],
    VoiceConditioningSeconds(float)[calls, agents, tools],
  UltravoxV1QueryApiKeyRequirement(class)[agents, calls, tools],
    Name(string)[agents, calls, tools],
  UltravoxV1QueryCorpusRequest(class)[corpora],
    Query(string)[corpora],
    MaxResults(int)[corpora],
  UltravoxV1SecurityOptions(class)[agents, calls, tools],
    Options(array)[agents, calls, tools],
      UltravoxV1SecurityRequirements(ref)[agents, calls, tools],
  UltravoxV1SecurityRequirement(class)[agents, calls, tools],
    QueryApiKey_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1QueryApiKeyRequirement(ref)[agents, calls, tools],
    HeaderApiKey_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1HeaderApiKeyRequirement(ref)[agents, calls, tools],
    HttpAuth_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1HttpAuthRequirement(ref)[agents, calls, tools],
  UltravoxV1SecurityRequirements(class)[agents, calls, tools],
    UltravoxV1SecurityRequirementsRequirements(class)[agents, calls, tools],
      UltravoxV1SecurityRequirement(ref)[agents, calls, tools],
    UltravoxCallTokenRequirement_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1UltravoxCallTokenRequirement(ref)[agents, calls, tools],
  UltravoxV1SelectedTool(class)[agents, calls],
    ToolId(string)[agents, calls],
    ToolName(string)[agents, calls],
    TemporaryTool_AllOf1Wrapped(allOf)[agents, calls],
      UltravoxV1BaseToolDefinition(ref)[agents, calls],
    NameOverride(string)[agents, calls],
    UltravoxV1SelectedToolAuthTokens(class)[agents, calls],
      AuthTokens(string)[agents, calls],
    UltravoxV1SelectedToolParameterOverrides(class)[agents, calls],
      GoogleProtobufValue(ref)[agents, calls],
    TransitionId(string)[agents, calls],
  UltravoxV1SourceStats(class)[corpora],
    UltravoxV1SourceStatsStatus(enum)[corpora],
    LastUpdated(DateTime)[corpora],
    NumDocs(int)[corpora],
  UltravoxV1StartAgentCallRequest(class)[agents],
    UltravoxV1StartAgentCallRequestTemplateContext(class)[agents],
    InitialMessages(array)[agents],
      UltravoxV1Message(ref)[agents],
    UltravoxV1StartAgentCallRequestMetadata(class)[agents],
      Metadata(string)[agents],
    Medium_AllOf1Wrapped3(allOf)[agents],
      UltravoxV1CallMedium(ref)[agents],
    JoinTimeout(string)[agents],
    MaxDuration(string)[agents],
    RecordingEnabled(bool)[agents],
    UltravoxV1StartAgentCallRequestInitialOutputMedium(enum)[agents],
    FirstSpeakerSettings_AllOf1Wrapped3(allOf)[agents],
      UltravoxV1FirstSpeakerSettings(ref)[agents],
    UltravoxV1StartAgentCallRequestExperimentalSettings(class)[agents],
  UltravoxV1StartCallRequest(class)[calls],
    SystemPrompt(string)[calls],
    Temperature(float)[calls],
    Model(string)[calls],
    Voice(string)[calls],
    ExternalVoice_AllOf1Wrapped(allOf)[calls],
      UltravoxV1ExternalVoice(ref)[calls],
    LanguageHint(string)[calls],
    InitialMessages(array)[calls],
      UltravoxV1Message(ref)[calls],
    JoinTimeout(string)[calls],
    MaxDuration(string)[calls],
    TimeExceededMessage(string)[calls],
    InactivityMessages(array)[calls],
      UltravoxV1TimedMessage(ref)[calls],
    SelectedTools(array)[calls],
      UltravoxV1SelectedTool(ref)[calls],
    Medium_AllOf1Wrapped4(allOf)[calls],
      UltravoxV1CallMedium(ref)[calls],
    RecordingEnabled(bool)[calls],
    UltravoxV1StartCallRequestFirstSpeaker(enum)[calls],
    TranscriptOptional(bool)[calls],
    UltravoxV1StartCallRequestInitialOutputMedium(enum)[calls],
    VadSettings_AllOf1Wrapped3(allOf)[calls],
      UltravoxV1VadSettings(ref)[calls],
    FirstSpeakerSettings_AllOf1Wrapped4(allOf)[calls],
      UltravoxV1FirstSpeakerSettings(ref)[calls],
    UltravoxV1StartCallRequestExperimentalSettings(class)[calls],
    UltravoxV1StartCallRequestMetadata(class)[calls],
      Metadata(string)[calls],
    UltravoxV1StartCallRequestInitialState(class)[calls],
  UltravoxV1StaticParameter(class)[agents, calls, tools],
    Name(string)[agents, calls, tools],
    UltravoxV1StaticParameterLocation(enum)[agents, calls, tools],
    Value_AllOf1Wrapped(allOf)[agents, calls, tools],
      GoogleProtobufValue(ref)[agents, calls, tools],
  UltravoxV1StaticToolResponse(class)[agents, calls, tools],
    ResponseText(string)[agents, calls, tools],
  UltravoxV1TimedMessage(class)[agents, calls, tools],
    Duration(string)[agents, calls, tools],
    Message(string)[agents, calls, tools],
    UltravoxV1TimedMessageEndBehavior(enum)[agents, calls, tools],
  UltravoxV1ToolRequirements(class)[agents, calls, tools],
    HttpSecurityOptions_AllOf1Wrapped(allOf)[agents, calls, tools],
      UltravoxV1SecurityOptions(ref)[agents, calls, tools],
    RequiredParameterOverrides(array)[agents, calls, tools],
      RequiredParameterOverridesItem(string)[agents, calls, tools],
  UltravoxV1UltravoxCallTokenRequirement(class)[agents, calls, tools],
    Scopes(array)[agents, calls, tools],
      ScopesItem(string)[agents, calls, tools],
  UltravoxV1UploadSpec(class)[corpora],
    DocumentIds(array)[corpora],
      DocumentIdsItem(string)[corpora],
  UltravoxV1VadSettings(class)[agents, calls, tools],
    TurnEndpointDelay(string)[agents, calls, tools],
    MinimumTurnDuration(string)[agents, calls, tools],
    MinimumInterruptionDuration(string)[agents, calls, tools],
    FrameActivationThreshold(float)[agents, calls, tools],
  Agent(ref)[agents],
  PatchedAgent(ref)[agents],
  UltravoxV1StartAgentCallRequest(ref)[agents],
  APIKeyCreate(ref)[api_keys],
  APIKey(ref)[api_keys],
  PatchedAPIKey(ref)[api_keys],
  UltravoxV1StartCallRequest(ref)[calls],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1QueryCorpusRequest(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  CorpusUploadsRequest(ref)[corpora],
  Tool(ref)[tools],
  ToolsCreateRequest(class)[tools],
    File(byte[])[tools],
  ToolDetail(ref)[tools],
  VoicesCreateRequest(class)[voices],
    File(byte[])[voices],
    Name(string)[voices],
    Description(string)[voices],
  Webhook(ref)[webhooks],
  Webhook(ref)[webhooks],
  PatchedWebhook(ref)[webhooks],
  AccountsMeUsageCallsRetrieveDurationMax(string)[accounts],
  AccountsMeUsageCallsRetrieveDurationMin(string)[accounts],
  AccountsMeUsageCallsRetrieveFromDate(DateTime)[accounts],
  AccountsMeUsageCallsRetrieveMetadata(class)[accounts],
    AccountsMeUsageCallsRetrieveMetadata(string)[accounts],
  AccountsMeUsageCallsRetrieveSearch(string)[accounts],
  AccountsMeUsageCallsRetrieveToDate(DateTime)[accounts],
  AccountsMeUsageCallsRetrieveVoiceId(string)[accounts],
  AgentsListCursor(string)[agents],
  AgentsListPageSize(int)[agents],
  AgentsRetrieveAgentId(string)[agents],
  AgentsPartialUpdateAgentId(string)[agents],
  AgentsDestroyAgentId(string)[agents],
  AgentsCallsListAgentId(string)[agents],
  AgentsCallsListCursor(string)[agents],
  AgentsCallsListPageSize(int)[agents],
  AgentsCallsCreateAgentId(string)[agents],
  ApiKeysListCursor(string)[api_keys],
  ApiKeysListPageSize(int)[api_keys],
  ApiKeysRetrieveApiKeyPrefix(string)[api_keys],
  ApiKeysUpdateApiKeyPrefix(string)[api_keys],
  ApiKeysPartialUpdateApiKeyPrefix(string)[api_keys],
  ApiKeysDestroyApiKeyPrefix(string)[api_keys],
  CallsListCursor(string)[calls],
  CallsListDurationMax(string)[calls],
  CallsListDurationMin(string)[calls],
  CallsListFromDate(DateTime)[calls],
  CallsListMetadata(class)[calls],
    CallsListMetadata(string)[calls],
  CallsListPageSize(int)[calls],
  CallsListSearch(string)[calls],
  CallsListSort(string)[calls],
  CallsListToDate(DateTime)[calls],
  CallsListVoiceId(string)[calls],
  CallsCreateEnableGreetingPrompt(bool)[calls],
  CallsCreatePriorCallId(string)[calls],
  CallsRetrieveCallId(string)[calls],
  CallsDestroyCallId(string)[calls],
  CallsMessagesListCallId(string)[calls],
  CallsMessagesListCursor(string)[calls],
  CallsMessagesListPageSize(int)[calls],
  CallsRecordingRetrieveCallId(string)[calls],
  CallsStagesListCallId(string)[calls],
  CallsStagesListCursor(string)[calls],
  CallsStagesListPageSize(int)[calls],
  CallsStagesRetrieveCallId(string)[calls],
  CallsStagesRetrieveCallStageId(string)[calls],
  CallsStagesMessagesListCallId(string)[calls],
  CallsStagesMessagesListCallStageId(string)[calls],
  CallsStagesMessagesListCursor(string)[calls],
  CallsStagesMessagesListPageSize(int)[calls],
  CallsStagesMessagesAudioRetrieveCallId(string)[calls],
  CallsStagesMessagesAudioRetrieveCallStageId(string)[calls],
  CallsStagesMessagesAudioRetrieveCallStageMessageIndex(int)[calls],
  CallsStagesToolsListCallId(string)[calls],
  CallsStagesToolsListCallStageId(string)[calls],
  CallsToolsListCallId(string)[calls],
  CorporaListCursor(string)[corpora],
  CorporaListPageSize(int)[corpora],
  CorporaRetrieveCorpusId(string)[corpora],
  CorporaUpdateCorpusId(string)[corpora],
  CorporaPartialUpdateCorpusId(string)[corpora],
  CorporaDestroyCorpusId(string)[corpora],
  CorporaQueryCorpusId(string)[corpora],
  CorporaSourcesListCorpusId(string)[corpora],
  CorporaSourcesListCursor(string)[corpora],
  CorporaSourcesListPageSize(int)[corpora],
  CorporaSourcesCreateCorpusId(string)[corpora],
  CorporaSourcesRetrieveCorpusId(string)[corpora],
  CorporaSourcesRetrieveSourceId(string)[corpora],
  CorporaSourcesUpdateCorpusId(string)[corpora],
  CorporaSourcesUpdateSourceId(string)[corpora],
  CorporaSourcesPartialUpdateCorpusId(string)[corpora],
  CorporaSourcesPartialUpdateSourceId(string)[corpora],
  CorporaSourcesDestroyCorpusId(string)[corpora],
  CorporaSourcesDestroySourceId(string)[corpora],
  CorporaSourcesDocumentsListCorpusId(string)[corpora],
  CorporaSourcesDocumentsListCursor(string)[corpora],
  CorporaSourcesDocumentsListPageSize(int)[corpora],
  CorporaSourcesDocumentsListSourceId(string)[corpora],
  CorporaSourcesDocumentsRetrieveCorpusId(string)[corpora],
  CorporaSourcesDocumentsRetrieveDocumentId(string)[corpora],
  CorporaSourcesDocumentsRetrieveSourceId(string)[corpora],
  CorporaUploadsCreateCorpusId(string)[corpora],
  DeletedCallsListCursor(string)[deleted_calls],
  DeletedCallsListDurationMax(string)[deleted_calls],
  DeletedCallsListDurationMin(string)[deleted_calls],
  DeletedCallsListFromDate(DateTime)[deleted_calls],
  DeletedCallsListMetadata(class)[deleted_calls],
    DeletedCallsListMetadata(string)[deleted_calls],
  DeletedCallsListPageSize(int)[deleted_calls],
  DeletedCallsListSearch(string)[deleted_calls],
  DeletedCallsListToDate(DateTime)[deleted_calls],
  DeletedCallsListVoiceId(string)[deleted_calls],
  DeletedCallsRetrieveCallId(string)[deleted_calls],
  ModelsListCursor(string)[models],
  ModelsListPageSize(int)[models],
  SchemaRetrieveFormat(enum)[schema],
  SchemaRetrieveLang(enum)[schema],
  ToolsListCursor(string)[tools],
  ToolsListOwnership(string)[tools],
  ToolsListPageSize(int)[tools],
  ToolsListSearch(string)[tools],
  ToolsRetrieveToolId(string)[tools],
  ToolsUpdateToolId(string)[tools],
  ToolsDestroyToolId(string)[tools],
  ToolsHistoryListCursor(string)[tools],
  ToolsHistoryListPageSize(int)[tools],
  ToolsHistoryListToolId(string)[tools],
  ToolsTestCreateToolId(string)[tools],
  VoicesListCursor(string)[voices],
  VoicesListOwnership(string)[voices],
  VoicesListPageSize(int)[voices],
  VoicesListSearch(string)[voices],
  VoicesRetrieveVoiceId(string)[voices],
  VoicesDestroyVoiceId(string)[voices],
  WebhooksListCursor(string)[webhooks],
  WebhooksListPageSize(int)[webhooks],
  WebhooksRetrieveWebhookId(string)[webhooks],
  WebhooksUpdateWebhookId(string)[webhooks],
  WebhooksPartialUpdateWebhookId(string)[webhooks],
  WebhooksDestroyWebhookId(string)[webhooks],
  AccountsListResponse(array)[accounts],
    Account(ref)[accounts],
  Account(ref)[accounts],
  CallUsage(ref)[accounts],
  PaginatedAgentList(ref)[agents],
  Agent(ref)[agents],
  Agent(ref)[agents],
  Agent(ref)[agents],
  PaginatedCallList(ref)[agents],
  Call(ref)[agents],
  PaginatedAPIKeyList(ref)[api_keys],
  APIKeyCreate(ref)[api_keys],
  APIKey(ref)[api_keys],
  APIKey(ref)[api_keys],
  APIKey(ref)[api_keys],
  PaginatedCallList(ref)[calls],
  Call(ref)[calls],
  Call(ref)[calls],
  PaginatedultravoxV1MessageList(ref)[calls],
  CallsRecordingRetrieveResponse(byte[])[calls],
  PaginatedCallStageList(ref)[calls],
  CallStage(ref)[calls],
  PaginatedultravoxV1MessageList(ref)[calls],
  CallsStagesToolsListResponse(array)[calls],
    CallTool(ref)[calls],
  CallsToolsListResponse(array)[calls],
    CallTool(ref)[calls],
  PaginatedultravoxV1CorpusList(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  UltravoxV1Corpus(ref)[corpora],
  CorporaQueryResponse(array)[corpora],
    UltravoxV1CorpusQueryResult(ref)[corpora],
  PaginatedultravoxV1CorpusSourceList(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  UltravoxV1CorpusSource(ref)[corpora],
  PaginatedultravoxV1CorpusDocumentList(ref)[corpora],
  UltravoxV1CorpusDocument(ref)[corpora],
  CorpusUploadsResponse(ref)[corpora],
  PaginatedCallTombstoneList(ref)[deleted_calls],
  CallTombstone(ref)[deleted_calls],
  PaginatedModelAliasList(ref)[models],
  SchemaRetrieveResponse(class)[schema],
    SchemaRetrieveResponse2(class)[schema],
  SchemaRetrieveResponse3(class)[schema],
    SchemaRetrieveResponse4(class)[schema],
  SchemaRetrieveResponse5(class)[schema],
    SchemaRetrieveResponse6(class)[schema],
  SchemaRetrieveResponse7(class)[schema],
    SchemaRetrieveResponse8(class)[schema],
  PaginatedToolList(ref)[tools],
  Tool(ref)[tools],
  ToolDetail(ref)[tools],
  ToolDetail(ref)[tools],
  PaginatedToolHistoryList(ref)[tools],
  ToolsTestCreateResponse(class)[tools],
  PaginatedVoiceList(ref)[voices],
  Voice(ref)[voices],
  Voice(ref)[voices],
  PaginatedWebhookList(ref)[webhooks],
  Webhook(ref)[webhooks],
  Webhook(ref)[webhooks],
  Webhook(ref)[webhooks],
  Webhook(ref)[webhooks]
]