[
  CreateMessageRequest(class)[Messages],
    Model(anyOf)[Messages],
      ModelVariant1(string)[Messages],
      CreateMessageRequestModel(enum)[Messages],
    Messages(array)[Messages],
      Message(ref)[Messages],
    MaxTokens(int)[Messages],
    CreateMessageRequestMetadata(ref)[Messages],
    StopSequences(array)[Messages],
      StopSequencesItem(string)[Messages],
    System(string)[Messages],
    Temperature(double)[Messages],
    ToolChoice(ref)[Messages],
    Tools(array)[Messages],
      Tool(ref)[Messages],
    TopK(int)[Messages],
    TopP(double)[Messages],
    Stream(bool)[Messages],
  Message(class)[Messages],
    Id(string)[Messages],
    Content(oneOf)[Messages],
      ContentVariant1(string)[Messages],
      ContentVariant2(array)[Messages],
        Block(ref)[Messages],
    MessageRole(ref)[Messages],
    Model(string)[Messages],
    StopReason(ref)[Messages],
    StopSequence(string)[Messages],
    Type(string)[Messages],
    Usage(ref)[Messages],
  Block(oneOf)[Messages],
    BlockDiscriminator(class)[Messages],
      BlockDiscriminatorType(enum)[Messages],
    TextBlock(ref)[Messages],
    ImageBlock(ref)[Messages],
    ToolUseBlock(ref)[Messages],
    ToolResultBlock(ref)[Messages],
  TextBlock(class)[Messages],
    Text(string)[Messages],
    Type(string)[Messages],
  ImageBlock(class)[Messages],
    ImageBlockSource(ref)[Messages],
    Type(string)[Messages],
  ImageBlockSource(class)[Messages],
    Data(string)[Messages],
    ImageBlockSourceMediaType(enum)[Messages],
    ImageBlockSourceType(enum)[Messages],
  ToolUseBlock(class)[Messages],
    Id(string)[Messages],
    Name(string)[Messages],
    ToolUseBlockInput(class)[Messages],
    Type(string)[Messages],
  ToolResultBlock(class)[Messages],
    ToolUseId(string)[Messages],
    Content2(oneOf)[Messages],
      ContentVariant1(string)[Messages],
      ContentVariant2(array)[Messages],
        Block(ref)[Messages],
    IsError(bool)[Messages],
    Type(string)[Messages],
  MessageRole(enum)[Messages],
  StopReason(enum)[Messages],
  Usage(class)[Messages],
    InputTokens(int)[Messages],
    OutputTokens(int)[Messages],
  CreateMessageRequestMetadata(class)[Messages],
    UserId(string)[Messages],
  ToolChoice(class)[Messages],
    ToolChoiceType(ref)[Messages],
    Name(string)[Messages],
  ToolChoiceType(enum)[Messages],
  Tool(class)[Messages],
    Name(string)[Messages],
    Description(string)[Messages],
    ToolInputSchema(class)[Messages],
  MessageStreamEvent(class)[],
    MessageStreamEventDiscriminator(class)[],
      MessageStreamEventDiscriminatorType(enum)[],
    MessageStartEvent(ref)[],
    MessageDeltaEvent(ref)[],
    MessageStopEvent(ref)[],
    ContentBlockStartEvent(ref)[],
    ContentBlockDeltaEvent(ref)[],
    ContentBlockStopEvent(ref)[],
    PingEvent(ref)[],
  MessageStartEvent(class)[],
    Message(ref)[],
    MessageStreamEventType(ref)[],
  MessageStreamEventType(enum)[],
  MessageDeltaEvent(class)[],
    MessageDelta(ref)[],
    MessageStreamEventType(ref)[],
    MessageDeltaUsage(ref)[],
  MessageDelta(class)[],
    StopReason(ref)[],
    StopSequence(string)[],
  MessageDeltaUsage(class)[],
    OutputTokens(int)[],
  MessageStopEvent(class)[],
    MessageStreamEventType(ref)[],
  ContentBlockStartEvent(class)[],
    Block(ref)[],
    Index(int)[],
    MessageStreamEventType(ref)[],
  ContentBlockDeltaEvent(class)[],
    BlockDelta(ref)[],
    Index(int)[],
    MessageStreamEventType(ref)[],
  BlockDelta(oneOf)[],
    BlockDeltaDiscriminator(class)[],
      BlockDeltaDiscriminatorType(enum)[],
    TextBlockDelta(ref)[],
    InputJsonBlockDelta(ref)[],
  TextBlockDelta(class)[],
    Text(string)[],
    Type(string)[],
  InputJsonBlockDelta(class)[],
    PartialJson(string)[],
    Type(string)[],
  ContentBlockStopEvent(class)[],
    Index(int)[],
    MessageStreamEventType(ref)[],
  PingEvent(class)[],
    MessageStreamEventType(ref)[],
  CreateMessageRequest(ref)[Messages],
  Message(ref)[Messages]
]