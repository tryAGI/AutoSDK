[
  APIError(class)[Messages, Text Completions, Message Batches],
    APIErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  AuthenticationError(class)[Messages, Text Completions, Message Batches],
    AuthenticationErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  Base64ImageSource(class)[Messages, Message Batches],
    Base64ImageSourceType(enum)[Messages, Message Batches],
    Base64ImageSourceMediaType(enum)[Messages, Message Batches],
    Data(byte)[Messages, Message Batches],
  Base64PDFSource(class)[Messages, Message Batches],
    Base64PDFSourceType(enum)[Messages, Message Batches],
    Base64PDFSourceMediaType(enum)[Messages, Message Batches],
    Data(byte)[Messages, Message Batches],
  BetaAPIError(class)[Messages, Message Batches],
    BetaAPIErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaAuthenticationError(class)[Messages, Message Batches],
    BetaAuthenticationErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaBase64ImageSource(class)[Messages, Message Batches],
    BetaBase64ImageSourceType(enum)[Messages, Message Batches],
    BetaBase64ImageSourceMediaType(enum)[Messages, Message Batches],
    Data(byte)[Messages, Message Batches],
  BetaBase64PDFSource(class)[Messages, Message Batches],
    BetaBase64PDFSourceType(enum)[Messages, Message Batches],
    BetaBase64PDFSourceMediaType(enum)[Messages, Message Batches],
    Data(byte)[Messages, Message Batches],
  BetaBashTool20241022(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaBashTool20241022CacheControlDiscriminator(class)[Messages, Message Batches],
        BetaBashTool20241022CacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaBashTool20241022Type(enum)[Messages, Message Batches],
    BetaBashTool20241022Name(enum)[Messages, Message Batches],
  BetaCacheControlEphemeral(class)[Messages, Message Batches],
    BetaCacheControlEphemeralType(enum)[Messages, Message Batches],
  BetaBillingError(class)[Messages, Message Batches],
    BetaBillingErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaCanceledResult(class)[Message Batches],
    BetaCanceledResultType(enum)[Message Batches],
  BetaCitationsDelta(class)[],
    BetaCitationsDeltaType(enum)[],
    Citation(oneOf)[],
      BetaResponseCharLocationCitation(ref)[],
      BetaResponsePageLocationCitation(ref)[],
      BetaResponseContentBlockLocationCitation(ref)[],
      BetaCitationsDeltaCitationDiscriminator(class)[],
        BetaCitationsDeltaCitationDiscriminatorType(enum)[],
  BetaResponseCharLocationCitation(class)[Messages, Message Batches],
    BetaResponseCharLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartCharIndex(int)[Messages],
    EndCharIndex(int)[Messages],
  BetaResponsePageLocationCitation(class)[Messages, Message Batches],
    BetaResponsePageLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartPageNumber(int)[Messages],
    EndPageNumber(int)[Messages],
  BetaResponseContentBlockLocationCitation(class)[Messages, Message Batches],
    BetaResponseContentBlockLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartBlockIndex(int)[Messages],
    EndBlockIndex(int)[Messages],
  BetaComputerUseTool20241022(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped2(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaComputerUseTool20241022CacheControlDiscriminator(class)[Messages, Message Batches],
        BetaComputerUseTool20241022CacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaComputerUseTool20241022Type(enum)[Messages, Message Batches],
    BetaComputerUseTool20241022Name(enum)[Messages, Message Batches],
    DisplayHeightPx(int)[Messages, Message Batches],
    DisplayWidthPx(int)[Messages, Message Batches],
    DisplayNumber(int)[Messages, Message Batches],
  BetaContentBlockDeltaEvent(class)[],
    BetaContentBlockDeltaEventType(enum)[],
    Index(int)[],
    Delta(oneOf)[],
      BetaTextContentBlockDelta(ref)[],
      BetaInputJsonContentBlockDelta(ref)[],
      BetaCitationsDelta(ref)[],
      BetaContentBlockDeltaEventDeltaDiscriminator(class)[],
        BetaContentBlockDeltaEventDeltaDiscriminatorType(enum)[],
  BetaTextContentBlockDelta(class)[],
    BetaTextContentBlockDeltaType(enum)[],
    Text(string)[],
  BetaInputJsonContentBlockDelta(class)[],
    BetaInputJsonContentBlockDeltaType(enum)[],
    PartialJson(string)[],
  BetaContentBlockSource(class)[Messages, Message Batches],
    BetaContentBlockSourceType(enum)[Messages, Message Batches],
    Content(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        ContentVariant2Item(oneOf)[Messages, Message Batches],
          BetaRequestTextBlock(ref)[Messages, Message Batches],
          BetaRequestImageBlock(ref)[Messages, Message Batches],
          BetaContentBlockSourceContentVariant2ItemDiscriminator(class)[Messages, Message Batches],
            BetaContentBlockSourceContentVariant2ItemDiscriminatorType(enum)[],
  BetaRequestTextBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped5(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaRequestTextBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaRequestTextBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    Citations(array)[Messages, Message Batches],
      CitationsItem(oneOf)[Messages, Message Batches],
        BetaRequestCharLocationCitation(ref)[Messages, Message Batches],
        BetaRequestPageLocationCitation(ref)[Messages, Message Batches],
        BetaRequestContentBlockLocationCitation(ref)[Messages, Message Batches],
        BetaRequestTextBlockCitationDiscriminator(class)[Messages, Message Batches],
          BetaRequestTextBlockCitationDiscriminatorType(enum)[Messages, Message Batches],
    Text(string)[Messages, Message Batches],
    BetaRequestTextBlockType(enum)[Messages, Message Batches],
  BetaRequestCharLocationCitation(class)[Messages, Message Batches],
    BetaRequestCharLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartCharIndex(int)[Messages, Message Batches],
    EndCharIndex(int)[Messages, Message Batches],
  BetaRequestPageLocationCitation(class)[Messages, Message Batches],
    BetaRequestPageLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartPageNumber(int)[Messages, Message Batches],
    EndPageNumber(int)[Messages, Message Batches],
  BetaRequestContentBlockLocationCitation(class)[Messages, Message Batches],
    BetaRequestContentBlockLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartBlockIndex(int)[Messages, Message Batches],
    EndBlockIndex(int)[Messages, Message Batches],
  BetaRequestImageBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped4(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaRequestImageBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaRequestImageBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaRequestImageBlockType(enum)[Messages, Message Batches],
    Source_AllOf1Wrapped(allOf)[Messages, Message Batches],
      BetaBase64ImageSource(ref)[Messages, Message Batches],
  BetaContentBlockStartEvent(class)[],
    BetaContentBlockStartEventType(enum)[],
    Index(int)[],
    ContentBlock(oneOf)[],
      BetaResponseTextBlock(ref)[],
      BetaResponseToolUseBlock(ref)[],
      BetaContentBlockStartEventContentBlockDiscriminator(class)[],
        BetaContentBlockStartEventContentBlockDiscriminatorType(enum)[],
  BetaResponseTextBlock(class)[Messages, Message Batches],
    BetaResponseTextBlockType(enum)[Messages, Message Batches],
    Text(string)[Messages, Message Batches],
    Citations(array)[Messages, Message Batches],
      CitationsItem2(oneOf)[Messages, Message Batches],
        BetaResponseCharLocationCitation(ref)[Messages, Message Batches],
        BetaResponsePageLocationCitation(ref)[Messages, Message Batches],
        BetaResponseContentBlockLocationCitation(ref)[Messages, Message Batches],
        BetaResponseTextBlockCitationDiscriminator(class)[Messages, Message Batches],
          BetaResponseTextBlockCitationDiscriminatorType(enum)[Messages, Message Batches],
  BetaResponseToolUseBlock(class)[Messages, Message Batches],
    BetaResponseToolUseBlockType(enum)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    BetaResponseToolUseBlockInput(class)[Messages, Message Batches],
  BetaContentBlockStopEvent(class)[],
    BetaContentBlockStopEventType(enum)[],
    Index(int)[],
  BetaCountMessageTokensParams(class)[Messages],
    BetaToolChoice(ref)[Messages],
    Tools(array)[Messages],
      ToolsItem(oneOf)[Messages],
        BetaTool(ref)[Messages],
        BetaComputerUseTool20241022(ref)[Messages],
        BetaBashTool20241022(ref)[Messages],
        BetaTextEditor20241022(ref)[Messages],
        BetaCountMessageTokensParamsToolDiscriminator(class)[Messages],
          BetaCountMessageTokensParamsToolDiscriminatorType(enum)[Messages],
    Messages(array)[Messages],
      BetaInputMessage(ref)[Messages],
    System(anyOf)[Messages],
      SystemVariant1(string)[Messages],
      SystemVariant2(array)[Messages],
        BetaRequestTextBlock(ref)[Messages],
    Model(ref)[Messages],
  BetaToolChoice(oneOf)[Messages, Message Batches],
    BetaToolChoiceAuto(ref)[Messages, Message Batches],
    BetaToolChoiceAny(ref)[Messages, Message Batches],
    BetaToolChoiceTool(ref)[Messages, Message Batches],
    BetaToolChoiceDiscriminator(class)[Messages, Message Batches],
      BetaToolChoiceDiscriminatorType(enum)[Messages, Message Batches],
  BetaToolChoiceAuto(class)[Messages, Message Batches],
    BetaToolChoiceAutoType(enum)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  BetaToolChoiceAny(class)[Messages, Message Batches],
    BetaToolChoiceAnyType(enum)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  BetaToolChoiceTool(class)[Messages, Message Batches],
    BetaToolChoiceToolType(enum)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  BetaTool(class)[Messages, Message Batches],
    BetaToolType(enum)[Messages, Message Batches],
    Description(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    InputSchema_AllOf1Wrapped(allOf)[Messages, Message Batches],
      BetaInputSchema(ref)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped9(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaToolCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaToolCacheControlDiscriminatorType(enum)[Messages, Message Batches],
  BetaInputSchema(class)[Messages, Message Batches],
    BetaInputSchemaType(enum)[Messages, Message Batches],
    BetaInputSchemaProperties(class)[Messages, Message Batches],
  BetaTextEditor20241022(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped8(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaTextEditor20241022CacheControlDiscriminator(class)[Messages, Message Batches],
        BetaTextEditor20241022CacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaTextEditor20241022Type(enum)[Messages, Message Batches],
    BetaTextEditor20241022Name(enum)[Messages, Message Batches],
  BetaInputMessage(class)[Messages, Message Batches],
    BetaInputMessageRole(enum)[Messages, Message Batches],
    Content2(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        BetaInputContentBlock(ref)[Messages, Message Batches],
    BetaInputMessageDiscriminator(class)[Messages, Message Batches],
      Role(string)[Messages, Message Batches],
  BetaInputContentBlock(oneOf)[Messages, Message Batches],
    BetaRequestTextBlock(ref)[Messages, Message Batches],
    BetaRequestImageBlock(ref)[Messages, Message Batches],
    BetaRequestToolUseBlock(ref)[Messages, Message Batches],
    BetaRequestToolResultBlock(ref)[Messages, Message Batches],
    BetaRequestDocumentBlock(ref)[Messages, Message Batches],
    BetaInputContentBlockDiscriminator(class)[Messages, Message Batches],
      BetaInputContentBlockDiscriminatorType(enum)[Messages, Message Batches],
  BetaRequestToolUseBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped7(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaRequestToolUseBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaRequestToolUseBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaRequestToolUseBlockType(enum)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    BetaRequestToolUseBlockInput(class)[Messages, Message Batches],
  BetaRequestToolResultBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped6(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaRequestToolResultBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaRequestToolResultBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaRequestToolResultBlockType(enum)[Messages, Message Batches],
    ToolUseId(string)[Messages, Message Batches],
    IsError(bool)[Messages, Message Batches],
    Content3(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        ContentVariant2Item2(oneOf)[Messages, Message Batches],
          BetaRequestTextBlock(ref)[Messages, Message Batches],
          BetaRequestImageBlock(ref)[Messages, Message Batches],
          BetaRequestToolResultBlockContentVariant2ItemDiscriminator(class)[Messages, Message Batches],
            BetaRequestToolResultBlockContentVariant2ItemDiscriminatorType(enum)[Messages, Message Batches],
  BetaRequestDocumentBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped3(oneOf)[Messages, Message Batches],
      BetaCacheControlEphemeral(ref)[Messages, Message Batches],
      BetaRequestDocumentBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        BetaRequestDocumentBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    BetaRequestDocumentBlockType(enum)[Messages, Message Batches],
    Source(oneOf)[Messages, Message Batches],
      BetaBase64PDFSource(ref)[Messages, Message Batches],
      BetaPlainTextSource(ref)[Messages, Message Batches],
      BetaContentBlockSource(ref)[Messages, Message Batches],
      BetaRequestDocumentBlockSourceDiscriminator(class)[Messages, Message Batches],
        BetaRequestDocumentBlockSourceDiscriminatorType(enum)[Messages, Message Batches],
    Title(string)[Messages, Message Batches],
    Context(string)[Messages, Message Batches],
    BetaRequestCitationsConfig(ref)[Messages, Message Batches],
  BetaPlainTextSource(class)[Messages, Message Batches],
    BetaPlainTextSourceType(enum)[Messages, Message Batches],
    BetaPlainTextSourceMediaType(enum)[Messages, Message Batches],
    Data(string)[Messages, Message Batches],
  BetaRequestCitationsConfig(class)[Messages, Message Batches],
    Enabled(bool)[Messages, Message Batches],
  Model(anyOf)[Messages, Text Completions, Message Batches],
    ModelVariant1(string)[Messages, Text Completions, Message Batches],
    ModelVariant2(enum)[Messages, Text Completions, Message Batches],
    ModelVariant3(enum)[Messages, Text Completions, Message Batches],
    ModelVariant4(enum)[Messages, Text Completions, Message Batches],
    ModelVariant5(enum)[Messages, Text Completions, Message Batches],
    ModelVariant6(enum)[Messages, Text Completions, Message Batches],
    ModelVariant7(enum)[Messages, Text Completions, Message Batches],
    ModelVariant8(enum)[Messages, Text Completions, Message Batches],
    ModelVariant9(enum)[Messages, Text Completions, Message Batches],
    ModelVariant10(enum)[Messages, Text Completions, Message Batches],
    ModelVariant11(enum)[Messages, Text Completions, Message Batches],
    ModelVariant12(enum)[Messages, Text Completions, Message Batches],
  BetaCountMessageTokensResponse(class)[Messages],
    InputTokens(int)[Messages],
  BetaCreateMessageBatchParams(class)[Message Batches],
    Requests(array)[Message Batches],
      BetaMessageBatchIndividualRequestParams(ref)[Message Batches],
  BetaMessageBatchIndividualRequestParams(class)[Message Batches],
    CustomId(string)[Message Batches],
    Params_AllOf1Wrapped(allOf)[Message Batches],
      BetaCreateMessageParams(ref)[Message Batches],
  BetaCreateMessageParams(class)[Messages, Message Batches],
    Model(ref)[Messages, Message Batches],
    Messages(array)[Messages, Message Batches],
      BetaInputMessage(ref)[Messages, Message Batches],
    MaxTokens(int)[Messages, Message Batches],
    Metadata_AllOf1Wrapped(allOf)[Messages, Message Batches],
      BetaMetadata(ref)[Messages, Message Batches],
    StopSequences(array)[Messages, Message Batches],
      StopSequencesItem(string)[Messages, Message Batches],
    Stream(bool)[Messages, Message Batches],
    System2(anyOf)[Messages, Message Batches],
      SystemVariant1(string)[Messages, Message Batches],
      SystemVariant2(array)[Messages, Message Batches],
        BetaRequestTextBlock(ref)[Messages, Message Batches],
    Temperature(double)[Messages, Message Batches],
    BetaToolChoice(ref)[Messages, Message Batches],
    Tools(array)[Messages, Message Batches],
      ToolsItem2(oneOf)[Messages, Message Batches],
        BetaTool(ref)[Messages, Message Batches],
        BetaComputerUseTool20241022(ref)[Messages, Message Batches],
        BetaBashTool20241022(ref)[Messages, Message Batches],
        BetaTextEditor20241022(ref)[Messages, Message Batches],
        BetaCreateMessageParamsToolDiscriminator(class)[Messages, Message Batches],
          BetaCreateMessageParamsToolDiscriminatorType(enum)[Messages, Message Batches],
    TopK(int)[Messages, Message Batches],
    TopP(double)[Messages, Message Batches],
  BetaMetadata(class)[Messages, Message Batches],
    UserId(string)[Messages, Message Batches],
  BetaDeleteMessageBatchResponse(class)[Message Batches],
    Id(string)[Message Batches],
    BetaDeleteMessageBatchResponseType(enum)[Message Batches],
  BetaErrorResponse(class)[Messages, Message Batches],
    BetaErrorResponseType(enum)[Messages, Message Batches],
    Error(oneOf)[Messages, Message Batches],
      BetaInvalidRequestError(ref)[Messages, Message Batches],
      BetaAuthenticationError(ref)[Messages, Message Batches],
      BetaBillingError(ref)[Messages, Message Batches],
      BetaPermissionError(ref)[Messages, Message Batches],
      BetaNotFoundError(ref)[Messages, Message Batches],
      BetaRateLimitError(ref)[Messages, Message Batches],
      BetaGatewayTimeoutError(ref)[Messages, Message Batches],
      BetaAPIError(ref)[Messages, Message Batches],
      BetaOverloadedError(ref)[Messages, Message Batches],
      BetaErrorResponseErrorDiscriminator(class)[Messages, Message Batches],
        BetaErrorResponseErrorDiscriminatorType(enum)[Messages, Message Batches],
  BetaInvalidRequestError(class)[Messages, Message Batches],
    BetaInvalidRequestErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaPermissionError(class)[Messages, Message Batches],
    BetaPermissionErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaNotFoundError(class)[Messages, Message Batches],
    BetaNotFoundErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaRateLimitError(class)[Messages, Message Batches],
    BetaRateLimitErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaGatewayTimeoutError(class)[Messages, Message Batches],
    BetaGatewayTimeoutErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaOverloadedError(class)[Messages, Message Batches],
    BetaOverloadedErrorType(enum)[Messages, Message Batches],
    Message(string)[Messages, Message Batches],
  BetaErroredResult(class)[Message Batches],
    BetaErroredResultType(enum)[Message Batches],
    BetaErrorResponse(ref)[Message Batches],
  BetaExpiredResult(class)[Message Batches],
    BetaExpiredResultType(enum)[Message Batches],
  BetaListResponseMessageBatch(class)[Message Batches],
    Data(array)[Message Batches],
      BetaMessageBatch(ref)[Message Batches],
    HasMore(bool)[Message Batches],
    FirstId(string)[Message Batches],
    LastId(string)[Message Batches],
  BetaMessageBatch(class)[Message Batches],
    Id(string)[Message Batches],
    BetaMessageBatchType(enum)[Message Batches],
    BetaMessageBatchProcessingStatus(enum)[Message Batches],
    RequestCounts_AllOf1Wrapped(allOf)[Message Batches],
      BetaRequestCounts(ref)[Message Batches],
    EndedAt(DateTime)[Message Batches],
    CreatedAt(DateTime)[Message Batches],
    ExpiresAt(DateTime)[Message Batches],
    ArchivedAt(DateTime)[Message Batches],
    CancelInitiatedAt(DateTime)[Message Batches],
    ResultsUrl(string)[Message Batches],
  BetaRequestCounts(class)[Message Batches],
    Processing(int)[Message Batches],
    Succeeded(int)[Message Batches],
    Errored(int)[Message Batches],
    Canceled(int)[Message Batches],
    Expired(int)[Message Batches],
  BetaListResponseModelInfo(class)[],
    Data(array)[],
      BetaModelInfo(ref)[],
    HasMore(bool)[],
    FirstId(string)[],
    LastId(string)[],
  BetaModelInfo(class)[],
    BetaModelInfoType(enum)[],
    Id(string)[],
    DisplayName(string)[],
    CreatedAt(DateTime)[],
  BetaMessage(class)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    BetaMessageType(enum)[Messages, Message Batches],
    BetaMessageRole(enum)[Messages, Message Batches],
    Content(array)[Messages, Message Batches],
      BetaContentBlock(ref)[Messages, Message Batches],
    Model(ref)[Messages, Message Batches],
    BetaMessageStopReason(enum)[Messages, Message Batches],
    StopSequence(string)[Messages, Message Batches],
    Usage_AllOf1Wrapped(allOf)[Messages, Message Batches],
      BetaUsage(ref)[Messages, Message Batches],
  BetaContentBlock(oneOf)[Messages, Message Batches],
    BetaResponseTextBlock(ref)[Messages, Message Batches],
    BetaResponseToolUseBlock(ref)[Messages, Message Batches],
    BetaContentBlockDiscriminator(class)[Messages, Message Batches],
      BetaContentBlockDiscriminatorType(enum)[Messages, Message Batches],
  BetaUsage(class)[Messages, Message Batches],
    InputTokens(int)[Messages, Message Batches],
    CacheCreationInputTokens(int)[Messages, Message Batches],
    CacheReadInputTokens(int)[Messages, Message Batches],
    OutputTokens(int)[Messages, Message Batches],
  BetaMessageBatchIndividualResponse(class)[Message Batches],
    CustomId(string)[Message Batches],
    Result(oneOf)[Message Batches],
      BetaSucceededResult(ref)[Message Batches],
      BetaErroredResult(ref)[Message Batches],
      BetaCanceledResult(ref)[Message Batches],
      BetaExpiredResult(ref)[Message Batches],
      BetaMessageBatchIndividualResponseResultDiscriminator(class)[Message Batches],
        BetaMessageBatchIndividualResponseResultDiscriminatorType(enum)[Message Batches],
  BetaSucceededResult(class)[Message Batches],
    BetaSucceededResultType(enum)[Message Batches],
    BetaMessage(ref)[Message Batches],
  BetaMessageDelta(class)[],
    BetaMessageDeltaStopReason(enum)[],
    StopSequence(string)[],
  BetaMessageDeltaEvent(class)[],
    BetaMessageDeltaEventType(enum)[],
    BetaMessageDelta(ref)[],
    Usage_AllOf1Wrapped2(allOf)[],
      BetaMessageDeltaUsage(ref)[],
  BetaMessageDeltaUsage(class)[],
    OutputTokens(int)[],
  BetaMessageStartEvent(class)[],
    BetaMessageStartEventType(enum)[],
    BetaMessage(ref)[],
  BetaMessageStopEvent(class)[],
    BetaMessageStopEventType(enum)[],
  BetaMessageStreamEvent(oneOf)[],
    BetaMessageStartEvent(ref)[],
    BetaMessageDeltaEvent(ref)[],
    BetaMessageStopEvent(ref)[],
    BetaContentBlockStartEvent(ref)[],
    BetaContentBlockDeltaEvent(ref)[],
    BetaContentBlockStopEvent(ref)[],
    BetaMessageStreamEventDiscriminator(class)[],
      BetaMessageStreamEventDiscriminatorType(enum)[],
  BillingError(class)[Messages, Text Completions, Message Batches],
    BillingErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  CacheControlEphemeral(class)[Messages, Message Batches],
    CacheControlEphemeralType(enum)[Messages, Message Batches],
  CanceledResult(class)[Message Batches],
    CanceledResultType(enum)[Message Batches],
  CitationsDelta(class)[],
    CitationsDeltaType(enum)[],
    Citation2(oneOf)[],
      ResponseCharLocationCitation(ref)[],
      ResponsePageLocationCitation(ref)[],
      ResponseContentBlockLocationCitation(ref)[],
      CitationsDeltaCitationDiscriminator(class)[],
        CitationsDeltaCitationDiscriminatorType(enum)[],
  ResponseCharLocationCitation(class)[Messages, Message Batches],
    ResponseCharLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartCharIndex(int)[Messages],
    EndCharIndex(int)[Messages],
  ResponsePageLocationCitation(class)[Messages, Message Batches],
    ResponsePageLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartPageNumber(int)[Messages],
    EndPageNumber(int)[Messages],
  ResponseContentBlockLocationCitation(class)[Messages, Message Batches],
    ResponseContentBlockLocationCitationType(enum)[Messages],
    CitedText(string)[Messages],
    DocumentIndex(int)[Messages],
    DocumentTitle(string)[Messages],
    StartBlockIndex(int)[Messages],
    EndBlockIndex(int)[Messages],
  CompletionRequest(class)[Text Completions],
    Model(ref)[Text Completions],
    Prompt(string)[Text Completions],
    MaxTokensToSample(int)[Text Completions],
    StopSequences(array)[Text Completions],
      StopSequencesItem(string)[Text Completions],
    Temperature(double)[Text Completions],
    TopP(double)[Text Completions],
    TopK(int)[Text Completions],
    Metadata_AllOf1Wrapped2(allOf)[Text Completions],
      Metadata(ref)[Text Completions],
    Stream(bool)[Text Completions],
  Metadata(class)[Messages, Text Completions, Message Batches],
    UserId(string)[Messages, Text Completions, Message Batches],
  CompletionResponse(class)[Text Completions],
    CompletionResponseType(enum)[Text Completions],
    Id(string)[Text Completions],
    Completion(string)[Text Completions],
    StopReason(string)[Text Completions],
    Model(ref)[Text Completions],
  ContentBlockDeltaEvent(class)[],
    ContentBlockDeltaEventType(enum)[],
    Index(int)[],
    Delta2(oneOf)[],
      TextContentBlockDelta(ref)[],
      InputJsonContentBlockDelta(ref)[],
      CitationsDelta(ref)[],
      ContentBlockDeltaEventDeltaDiscriminator(class)[],
        ContentBlockDeltaEventDeltaDiscriminatorType(enum)[],
  TextContentBlockDelta(class)[],
    TextContentBlockDeltaType(enum)[],
    Text(string)[],
  InputJsonContentBlockDelta(class)[],
    InputJsonContentBlockDeltaType(enum)[],
    PartialJson(string)[],
  ContentBlockSource(class)[Messages, Message Batches],
    ContentBlockSourceType(enum)[Messages, Message Batches],
    Content4(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        ContentVariant2Item3(oneOf)[Messages, Message Batches],
          RequestTextBlock(ref)[Messages, Message Batches],
          RequestImageBlock(ref)[Messages, Message Batches],
          ContentBlockSourceContentVariant2ItemDiscriminator(class)[Messages, Message Batches],
            ContentBlockSourceContentVariant2ItemDiscriminatorType(enum)[],
  RequestTextBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped12(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      RequestTextBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        RequestTextBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    Citations(array)[Messages, Message Batches],
      CitationsItem3(oneOf)[Messages, Message Batches],
        RequestCharLocationCitation(ref)[Messages, Message Batches],
        RequestPageLocationCitation(ref)[Messages, Message Batches],
        RequestContentBlockLocationCitation(ref)[Messages, Message Batches],
        RequestTextBlockCitationDiscriminator(class)[Messages, Message Batches],
          RequestTextBlockCitationDiscriminatorType(enum)[Messages, Message Batches],
    Text(string)[Messages, Message Batches],
    RequestTextBlockType(enum)[Messages, Message Batches],
  RequestCharLocationCitation(class)[Messages, Message Batches],
    RequestCharLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartCharIndex(int)[Messages, Message Batches],
    EndCharIndex(int)[Messages, Message Batches],
  RequestPageLocationCitation(class)[Messages, Message Batches],
    RequestPageLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartPageNumber(int)[Messages, Message Batches],
    EndPageNumber(int)[Messages, Message Batches],
  RequestContentBlockLocationCitation(class)[Messages, Message Batches],
    RequestContentBlockLocationCitationType(enum)[Messages, Message Batches],
    CitedText(string)[Messages, Message Batches],
    DocumentIndex(int)[Messages, Message Batches],
    DocumentTitle(string)[Messages, Message Batches],
    StartBlockIndex(int)[Messages, Message Batches],
    EndBlockIndex(int)[Messages, Message Batches],
  RequestImageBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped11(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      RequestImageBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        RequestImageBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    RequestImageBlockType(enum)[Messages, Message Batches],
    Source_AllOf1Wrapped2(allOf)[Messages, Message Batches],
      Base64ImageSource(ref)[Messages, Message Batches],
  ContentBlockStartEvent(class)[],
    ContentBlockStartEventType(enum)[],
    Index(int)[],
    ContentBlock2(oneOf)[],
      ResponseTextBlock(ref)[],
      ResponseToolUseBlock(ref)[],
      ContentBlockStartEventContentBlockDiscriminator(class)[],
        ContentBlockStartEventContentBlockDiscriminatorType(enum)[],
  ResponseTextBlock(class)[Messages, Message Batches],
    ResponseTextBlockType(enum)[Messages, Message Batches],
    Text(string)[Messages, Message Batches],
    Citations(array)[Messages, Message Batches],
      CitationsItem4(oneOf)[Messages, Message Batches],
        ResponseCharLocationCitation(ref)[Messages, Message Batches],
        ResponsePageLocationCitation(ref)[Messages, Message Batches],
        ResponseContentBlockLocationCitation(ref)[Messages, Message Batches],
        ResponseTextBlockCitationDiscriminator(class)[Messages, Message Batches],
          ResponseTextBlockCitationDiscriminatorType(enum)[Messages, Message Batches],
  ResponseToolUseBlock(class)[Messages, Message Batches],
    ResponseToolUseBlockType(enum)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    ResponseToolUseBlockInput(class)[Messages, Message Batches],
  ContentBlockStopEvent(class)[],
    ContentBlockStopEventType(enum)[],
    Index(int)[],
  CountMessageTokensParams(class)[Messages],
    ToolChoice(ref)[Messages],
    Tools(array)[Messages],
      Tool(ref)[Messages],
    Messages(array)[Messages],
      InputMessage(ref)[Messages],
    System3(anyOf)[Messages],
      SystemVariant1(string)[Messages],
      SystemVariant2(array)[Messages],
        RequestTextBlock(ref)[Messages],
    Model(ref)[Messages],
  ToolChoice(oneOf)[Messages, Message Batches],
    ToolChoiceAuto(ref)[Messages, Message Batches],
    ToolChoiceAny(ref)[Messages, Message Batches],
    ToolChoiceTool(ref)[Messages, Message Batches],
    ToolChoiceDiscriminator(class)[Messages, Message Batches],
      ToolChoiceDiscriminatorType(enum)[Messages, Message Batches],
  ToolChoiceAuto(class)[Messages, Message Batches],
    ToolChoiceAutoType(enum)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  ToolChoiceAny(class)[Messages, Message Batches],
    ToolChoiceAnyType(enum)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  ToolChoiceTool(class)[Messages, Message Batches],
    ToolChoiceToolType(enum)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    DisableParallelToolUse(bool)[Messages, Message Batches],
  Tool(class)[Messages, Message Batches],
    Description(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    InputSchema_AllOf1Wrapped2(allOf)[Messages, Message Batches],
      InputSchema(ref)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped15(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      ToolCacheControlDiscriminator(class)[Messages, Message Batches],
        ToolCacheControlDiscriminatorType(enum)[Messages, Message Batches],
  InputSchema(class)[Messages, Message Batches],
    InputSchemaType(enum)[Messages, Message Batches],
    InputSchemaProperties(class)[Messages, Message Batches],
  InputMessage(class)[Messages, Message Batches],
    InputMessageRole(enum)[Messages, Message Batches],
    Content5(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        InputContentBlock(ref)[Messages, Message Batches],
    InputMessageDiscriminator(class)[Messages, Message Batches],
      Role(string)[Messages, Message Batches],
  InputContentBlock(oneOf)[Messages, Message Batches],
    RequestTextBlock(ref)[Messages, Message Batches],
    RequestImageBlock(ref)[Messages, Message Batches],
    RequestToolUseBlock(ref)[Messages, Message Batches],
    RequestToolResultBlock(ref)[Messages, Message Batches],
    RequestDocumentBlock(ref)[Messages, Message Batches],
    InputContentBlockDiscriminator(class)[Messages, Message Batches],
      InputContentBlockDiscriminatorType(enum)[Messages, Message Batches],
  RequestToolUseBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped14(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      RequestToolUseBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        RequestToolUseBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    RequestToolUseBlockType(enum)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    Name(string)[Messages, Message Batches],
    RequestToolUseBlockInput(class)[Messages, Message Batches],
  RequestToolResultBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped13(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      RequestToolResultBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        RequestToolResultBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    RequestToolResultBlockType(enum)[Messages, Message Batches],
    ToolUseId(string)[Messages, Message Batches],
    IsError(bool)[Messages, Message Batches],
    Content6(anyOf)[Messages, Message Batches],
      ContentVariant1(string)[Messages, Message Batches],
      ContentVariant2(array)[Messages, Message Batches],
        ContentVariant2Item4(oneOf)[Messages, Message Batches],
          RequestTextBlock(ref)[Messages, Message Batches],
          RequestImageBlock(ref)[Messages, Message Batches],
          RequestToolResultBlockContentVariant2ItemDiscriminator(class)[Messages, Message Batches],
            RequestToolResultBlockContentVariant2ItemDiscriminatorType(enum)[Messages, Message Batches],
  RequestDocumentBlock(class)[Messages, Message Batches],
    CacheControl_AllOf1Wrapped10(oneOf)[Messages, Message Batches],
      CacheControlEphemeral(ref)[Messages, Message Batches],
      RequestDocumentBlockCacheControlDiscriminator(class)[Messages, Message Batches],
        RequestDocumentBlockCacheControlDiscriminatorType(enum)[Messages, Message Batches],
    RequestDocumentBlockType(enum)[Messages, Message Batches],
    Source2(oneOf)[Messages, Message Batches],
      Base64PDFSource(ref)[Messages, Message Batches],
      PlainTextSource(ref)[Messages, Message Batches],
      ContentBlockSource(ref)[Messages, Message Batches],
      RequestDocumentBlockSourceDiscriminator(class)[Messages, Message Batches],
        RequestDocumentBlockSourceDiscriminatorType(enum)[Messages, Message Batches],
    Title(string)[Messages, Message Batches],
    Context(string)[Messages, Message Batches],
    RequestCitationsConfig(ref)[Messages, Message Batches],
  PlainTextSource(class)[Messages, Message Batches],
    PlainTextSourceType(enum)[Messages, Message Batches],
    PlainTextSourceMediaType(enum)[Messages, Message Batches],
    Data(string)[Messages, Message Batches],
  RequestCitationsConfig(class)[Messages, Message Batches],
    Enabled(bool)[Messages, Message Batches],
  CountMessageTokensResponse(class)[Messages],
    InputTokens(int)[Messages],
  CreateMessageBatchParams(class)[Message Batches],
    Requests(array)[Message Batches],
      MessageBatchIndividualRequestParams(ref)[Message Batches],
  MessageBatchIndividualRequestParams(class)[Message Batches],
    CustomId(string)[Message Batches],
    Params_AllOf1Wrapped2(allOf)[Message Batches],
      CreateMessageParams(ref)[Message Batches],
  CreateMessageParams(class)[Messages, Message Batches],
    Model(ref)[Messages, Message Batches],
    Messages(array)[Messages, Message Batches],
      InputMessage(ref)[Messages, Message Batches],
    MaxTokens(int)[Messages, Message Batches],
    Metadata_AllOf1Wrapped3(allOf)[Messages, Message Batches],
      Metadata(ref)[Messages, Message Batches],
    StopSequences(array)[Messages, Message Batches],
      StopSequencesItem(string)[Messages, Message Batches],
    Stream(bool)[Messages, Message Batches],
    System4(anyOf)[Messages, Message Batches],
      SystemVariant1(string)[Messages, Message Batches],
      SystemVariant2(array)[Messages, Message Batches],
        RequestTextBlock(ref)[Messages, Message Batches],
    Temperature(double)[Messages, Message Batches],
    ToolChoice(ref)[Messages, Message Batches],
    Tools(array)[Messages, Message Batches],
      Tool(ref)[Messages, Message Batches],
    TopK(int)[Messages, Message Batches],
    TopP(double)[Messages, Message Batches],
  DeleteMessageBatchResponse(class)[Message Batches],
    Id(string)[Message Batches],
    DeleteMessageBatchResponseType(enum)[Message Batches],
  ErrorResponse(class)[Messages, Text Completions, Message Batches],
    ErrorResponseType(enum)[Messages, Text Completions, Message Batches],
    Error2(oneOf)[Messages, Text Completions, Message Batches],
      InvalidRequestError(ref)[Messages, Text Completions, Message Batches],
      AuthenticationError(ref)[Messages, Text Completions, Message Batches],
      BillingError(ref)[Messages, Text Completions, Message Batches],
      PermissionError(ref)[Messages, Text Completions, Message Batches],
      NotFoundError(ref)[Messages, Text Completions, Message Batches],
      RateLimitError(ref)[Messages, Text Completions, Message Batches],
      GatewayTimeoutError(ref)[Messages, Text Completions, Message Batches],
      APIError(ref)[Messages, Text Completions, Message Batches],
      OverloadedError(ref)[Messages, Text Completions, Message Batches],
      ErrorResponseErrorDiscriminator(class)[Messages, Text Completions, Message Batches],
        ErrorResponseErrorDiscriminatorType(enum)[Messages, Text Completions, Message Batches],
  InvalidRequestError(class)[Messages, Text Completions, Message Batches],
    InvalidRequestErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  PermissionError(class)[Messages, Text Completions, Message Batches],
    PermissionErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  NotFoundError(class)[Messages, Text Completions, Message Batches],
    NotFoundErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  RateLimitError(class)[Messages, Text Completions, Message Batches],
    RateLimitErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  GatewayTimeoutError(class)[Messages, Text Completions, Message Batches],
    GatewayTimeoutErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  OverloadedError(class)[Messages, Text Completions, Message Batches],
    OverloadedErrorType(enum)[Messages, Text Completions, Message Batches],
    Message(string)[Messages, Text Completions, Message Batches],
  ErroredResult(class)[Message Batches],
    ErroredResultType(enum)[Message Batches],
    ErrorResponse(ref)[Message Batches],
  ExpiredResult(class)[Message Batches],
    ExpiredResultType(enum)[Message Batches],
  ListResponseMessageBatch(class)[Message Batches],
    Data(array)[Message Batches],
      MessageBatch(ref)[Message Batches],
    HasMore(bool)[Message Batches],
    FirstId(string)[Message Batches],
    LastId(string)[Message Batches],
  MessageBatch(class)[Message Batches],
    Id(string)[Message Batches],
    MessageBatchType(enum)[Message Batches],
    MessageBatchProcessingStatus(enum)[Message Batches],
    RequestCounts_AllOf1Wrapped2(allOf)[Message Batches],
      RequestCounts(ref)[Message Batches],
    EndedAt(DateTime)[Message Batches],
    CreatedAt(DateTime)[Message Batches],
    ExpiresAt(DateTime)[Message Batches],
    ArchivedAt(DateTime)[Message Batches],
    CancelInitiatedAt(DateTime)[Message Batches],
    ResultsUrl(string)[Message Batches],
  RequestCounts(class)[Message Batches],
    Processing(int)[Message Batches],
    Succeeded(int)[Message Batches],
    Errored(int)[Message Batches],
    Canceled(int)[Message Batches],
    Expired(int)[Message Batches],
  ListResponseModelInfo(class)[],
    Data(array)[],
      ModelInfo(ref)[],
    HasMore(bool)[],
    FirstId(string)[],
    LastId(string)[],
  ModelInfo(class)[],
    ModelInfoType(enum)[],
    Id(string)[],
    DisplayName(string)[],
    CreatedAt(DateTime)[],
  Message(class)[Messages, Message Batches],
    Id(string)[Messages, Message Batches],
    MessageType(enum)[Messages, Message Batches],
    MessageRole(enum)[Messages, Message Batches],
    Content(array)[Messages, Message Batches],
      ContentBlock3(ref)[Messages, Message Batches],
    Model(ref)[Messages, Message Batches],
    MessageStopReason(enum)[Messages, Message Batches],
    StopSequence(string)[Messages, Message Batches],
    Usage_AllOf1Wrapped3(allOf)[Messages, Message Batches],
      Usage(ref)[Messages, Message Batches],
  ContentBlock3(oneOf)[Messages, Message Batches],
    ResponseTextBlock(ref)[Messages, Message Batches],
    ResponseToolUseBlock(ref)[Messages, Message Batches],
    ContentBlockDiscriminator(class)[Messages, Message Batches],
      ContentBlockDiscriminatorType(enum)[Messages, Message Batches],
  Usage(class)[Messages, Message Batches],
    InputTokens(int)[Messages, Message Batches],
    CacheCreationInputTokens(int)[Messages, Message Batches],
    CacheReadInputTokens(int)[Messages, Message Batches],
    OutputTokens(int)[Messages, Message Batches],
  MessageBatchIndividualResponse(class)[Message Batches],
    CustomId(string)[Message Batches],
    Result2(oneOf)[Message Batches],
      SucceededResult(ref)[Message Batches],
      ErroredResult(ref)[Message Batches],
      CanceledResult(ref)[Message Batches],
      ExpiredResult(ref)[Message Batches],
      MessageBatchIndividualResponseResultDiscriminator(class)[Message Batches],
        MessageBatchIndividualResponseResultDiscriminatorType(enum)[Message Batches],
  SucceededResult(class)[Message Batches],
    SucceededResultType(enum)[Message Batches],
    Message(ref)[Message Batches],
  MessageDelta(class)[],
    MessageDeltaStopReason(enum)[],
    StopSequence(string)[],
  MessageDeltaEvent(class)[],
    MessageDeltaEventType(enum)[],
    MessageDelta(ref)[],
    Usage_AllOf1Wrapped4(allOf)[],
      MessageDeltaUsage(ref)[],
  MessageDeltaUsage(class)[],
    OutputTokens(int)[],
  MessageStartEvent(class)[],
    MessageStartEventType(enum)[],
    Message(ref)[],
  MessageStopEvent(class)[],
    MessageStopEventType(enum)[],
  MessageStreamEvent(oneOf)[],
    MessageStartEvent(ref)[],
    MessageDeltaEvent(ref)[],
    MessageStopEvent(ref)[],
    ContentBlockStartEvent(ref)[],
    ContentBlockDeltaEvent(ref)[],
    ContentBlockStopEvent(ref)[],
    MessageStreamEventDiscriminator(class)[],
      MessageStreamEventDiscriminatorType(enum)[],
  CreateMessageParamsWithoutStream(class)[],
    Model(ref)[],
    Messages(array)[],
      InputMessage(ref)[],
    MaxTokens(int)[],
    Metadata_AllOf1Wrapped4(allOf)[],
      Metadata(ref)[],
    StopSequences(array)[],
      StopSequencesItem(string)[],
    System5(anyOf)[],
      SystemVariant1(string)[],
      SystemVariant2(array)[],
        RequestTextBlock(ref)[],
    Temperature(double)[],
    ToolChoice(ref)[],
    Tools(array)[],
      Tool(ref)[],
    TopK(int)[],
    TopP(double)[],
  AnthropicBeta(anyOf)[],
    AnthropicBetaVariant1(string)[],
    AnthropicBetaEnum(enum)[],
  CreateMessageParams(ref)[Messages],
  CompletionRequest(ref)[Text Completions],
  CreateMessageBatchParams(ref)[Message Batches],
  CountMessageTokensParams(ref)[Messages],
  BetaCreateMessageParams(ref)[Messages],
  BetaCreateMessageBatchParams(ref)[Message Batches],
  BetaCountMessageTokensParams(ref)[Messages],
  MessagesPostAnthropicVersion(string)[Messages],
  CompletePostAnthropicVersion(string)[Text Completions],
  ModelsListBeforeId(string)[],
  ModelsListAfterId(string)[],
  ModelsListLimit(int)[],
  ModelsListAnthropicVersion(string)[],
  ModelsListXApiKey(string)[],
  ModelsGetModelId(string)[],
  ModelsGetAnthropicVersion(string)[],
  ModelsGetXApiKey(string)[],
  MessageBatchesPostAnthropicVersion(string)[Message Batches],
  MessageBatchesListBeforeId(string)[Message Batches],
  MessageBatchesListAfterId(string)[Message Batches],
  MessageBatchesListLimit(int)[Message Batches],
  MessageBatchesListAnthropicVersion(string)[Message Batches],
  MessageBatchesListXApiKey(string)[Message Batches],
  MessageBatchesRetrieveMessageBatchId(string)[Message Batches],
  MessageBatchesRetrieveAnthropicVersion(string)[Message Batches],
  MessageBatchesRetrieveXApiKey(string)[Message Batches],
  MessageBatchesDeleteMessageBatchId(string)[Message Batches],
  MessageBatchesDeleteAnthropicVersion(string)[Message Batches],
  MessageBatchesDeleteXApiKey(string)[Message Batches],
  MessageBatchesCancelMessageBatchId(string)[Message Batches],
  MessageBatchesCancelAnthropicVersion(string)[Message Batches],
  MessageBatchesResultsMessageBatchId(string)[Message Batches],
  MessageBatchesResultsAnthropicVersion(string)[Message Batches],
  MessageBatchesResultsXApiKey(string)[Message Batches],
  MessagesCountTokensPostAnthropicVersion(string)[Messages],
  BetaMessagesPostAnthropicBeta(string)[Messages],
    BetaMessagesPostAnthropicBetaItem(string)[Messages],
  BetaMessagesPostAnthropicVersion(string)[Messages],
  BetaModelsListBeforeId(string)[],
  BetaModelsListAfterId(string)[],
  BetaModelsListLimit(int)[],
  BetaModelsListAnthropicVersion(string)[],
  BetaModelsListXApiKey(string)[],
  BetaModelsGetModelId(string)[],
  BetaModelsGetAnthropicVersion(string)[],
  BetaModelsGetXApiKey(string)[],
  BetaMessageBatchesPostAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesPostAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesPostAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesListBeforeId(string)[Message Batches],
  BetaMessageBatchesListAfterId(string)[Message Batches],
  BetaMessageBatchesListLimit(int)[Message Batches],
  BetaMessageBatchesListAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesListAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesListAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesListXApiKey(string)[Message Batches],
  BetaMessageBatchesRetrieveMessageBatchId(string)[Message Batches],
  BetaMessageBatchesRetrieveAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesRetrieveAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesRetrieveAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesRetrieveXApiKey(string)[Message Batches],
  BetaMessageBatchesDeleteMessageBatchId(string)[Message Batches],
  BetaMessageBatchesDeleteAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesDeleteAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesDeleteAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesDeleteXApiKey(string)[Message Batches],
  BetaMessageBatchesCancelMessageBatchId(string)[Message Batches],
  BetaMessageBatchesCancelAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesCancelAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesCancelAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesResultsMessageBatchId(string)[Message Batches],
  BetaMessageBatchesResultsAnthropicBeta(string)[Message Batches],
    BetaMessageBatchesResultsAnthropicBetaItem(string)[Message Batches],
  BetaMessageBatchesResultsAnthropicVersion(string)[Message Batches],
  BetaMessageBatchesResultsXApiKey(string)[Message Batches],
  BetaMessagesCountTokensPostAnthropicBeta(string)[Messages],
    BetaMessagesCountTokensPostAnthropicBetaItem(string)[Messages],
  BetaMessagesCountTokensPostAnthropicVersion(string)[Messages],
  Message(ref)[Messages],
  ErrorResponse(ref)[Messages],
  CompletionResponse(ref)[Text Completions],
  ErrorResponse(ref)[Text Completions],
  ListResponseModelInfo(ref)[],
  ErrorResponse(ref)[],
  ModelInfo(ref)[],
  ErrorResponse(ref)[],
  MessageBatch(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  ListResponseMessageBatch(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  MessageBatch(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  DeleteMessageBatchResponse(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  MessageBatch(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  MessageBatchIndividualResponse(ref)[Message Batches],
  ErrorResponse(ref)[Message Batches],
  CountMessageTokensResponse(ref)[Messages],
  ErrorResponse(ref)[Messages],
  BetaMessage(ref)[Messages],
  BetaErrorResponse(ref)[Messages],
  BetaListResponseModelInfo(ref)[],
  BetaErrorResponse(ref)[],
  BetaModelInfo(ref)[],
  BetaErrorResponse(ref)[],
  BetaMessageBatch(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaListResponseMessageBatch(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaMessageBatch(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaDeleteMessageBatchResponse(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaMessageBatch(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaMessageBatchIndividualResponse(ref)[Message Batches],
  BetaErrorResponse(ref)[Message Batches],
  BetaCountMessageTokensResponse(ref)[Messages],
  BetaErrorResponse(ref)[Messages]
]